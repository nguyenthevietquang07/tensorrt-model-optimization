[project]
name = "tensorrt-model-optimization"
version = "0.1.0"
description = "Benchmark scaffold for model export and optional TensorRT acceleration."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
