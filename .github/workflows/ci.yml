name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Run unit tests
        run: python -m unittest discover -s tests
      - name: Run local benchmark demo
        run: python -m src.modelopt.benchmark --trials 25 --output /tmp/local_cpu_report.json
      - name: Run comparison report
        run: python scripts/compare_reports.py --baseline /tmp/local_cpu_report.json --candidate /tmp/local_cpu_report.json
